services:
  - type: web
    name: ai-housing-project
    runtime: python
    buildCommand: "cd frontend && npm install && npm run build && cd .. && pip install --upgrade pip setuptools wheel && pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000
