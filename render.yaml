services:
  - type: web
    name: ai-housing-project
    runtime: python
    buildCommand: "cd frontend && npm install && npm run build && cd .. && pip install --upgrade pip setuptools wheel && pip install numpy==1.26.4 && pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.4
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 5000
